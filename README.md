# 硬件工程师作品集

## 修改方法
1. 打开 `index.html`：修改首页姓名、简介、联系方式、项目标题和项目简介。
2. 打开 `style.css`：修改颜色、字体、卡片样式和布局。
3. 打开三个 `project-*.html`：修改对应项目的职责与技术内容。
4. 如果要加项目：复制一个项目页，再在 `index.html` 增加一个项目卡片链接。
5. 如果要放图片：把图片放进同一个文件夹，例如 `images/zynq.jpg`，然后在 HTML 中加入 `<img src="images/zynq.jpg" alt="Zynq项目">`。
